server:
  port: 4444
  servlet:
    contextPath: /api/security-service

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enable: true